# 优化数据侧边栏与时间轴交互

## 1. 重构 DataSidebar.vue 的数据加载与联动逻辑
- **增强“开封市”图层自动识别**：
    - 在 `confirmAddData` 方法中，不仅通过 `includes('开封市')` 检查，还将通过文件名（如包含“示例”）或检查 CSV 内容是否包含开封下属县区（如“兰考县”、“尉氏县”等）来更精准地触发开封市矢量图层的加载。
    - 确保在调用 `mapManager.handleRegionSelected` 前，`geoDataService` 已完成初始化。
- **完善数据添加流程**：
    - 优化 `parseFormText`，确保在加载模板数据后，字段映射（District 和 年份字段）能被自动准确识别，减少用户手动操作。
    - 确保在确认添加数据后，立即调用 `mapManager.updateLayerStyles()` 以在地图上渲染颜色。

## 2. 改进 TimeAxis.vue 的点击与拖拽交互
- **实现轨道点击跳转**：
    - 为时间轴轨道添加点击事件处理器，允许用户通过点击轨道直接跳转到特定年份，解决“点击滑块偏移”的体感问题。
- **优化滑块定位逻辑**：
    - 修正 `mousedown` 时的初始位置计算，确保滑块在被点击时不会发生微小跳变。
    - 确保 `tempProgress` 的更新与 `window` 级别的 `mousemove` 事件完全同步，提升拖拽灵敏度。

## 3. 视觉与交互细节打磨
- **侧边栏布局调整**：
    - 确认“已导入数据列表”始终处于“添加数据”按钮上方。
    - 优化 `el-dialog` 中的预览区域，确保手动编辑 CSV 文本时有良好的等宽字体显示和足够的滚动空间。
- **图层同步验证**：
    - 验证 `mapManager` 是否在数据添加后正确清理了旧的标注（Label），防止重复添加导致页面卡顿。

请确认以上计划，我将开始执行代码修改。